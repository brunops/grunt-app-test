{
  "name": "grunt-base-project",
  "version": "0.0.0",
  "description": "Base project using grunt",
  "main": "index.js",
  "scripts": {
    "preinstall": "npm install -g grunt-cli",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "grunt",
    "npm",
    "browserify"
  ],
  "author": "brunops",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-browserify": "^2.1.1",
    "grunt-contrib-jade": "^0.12.0"
  }
}
